# FPGA-x-CDS-NI
Developing an FPGA device that accelerated the processing of a CDS(Cognitive Dynamic System) + NI(Natural Intelligence) algorithm


Why FPGA:

Faster processing of the decision making
PAC’s can run in parallel(multiple features are run in parallel to get confidence up faster)
No longer connected to computer - Real world application usage
